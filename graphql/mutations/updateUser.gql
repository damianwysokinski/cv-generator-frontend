mutation UpdateUser(
    $id: String!
    $firstName: String!
    $lastName: String!
    $email: String!
) {
    updateUser(updateUserInput: {
        id: $id,
        firstName: $firstName,
        lastName: $lastName,
        email: $email,
    }) {
        id
        firstName
        lastName
        email
    }
}